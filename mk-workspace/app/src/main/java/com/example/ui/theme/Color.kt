package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Frosted Glass Theme Palette
val DeepBlackBg = Color(0xFF050505)         // Base space canvas background (frosted glass backing)
val DarkGraySurface200 = Color(0x12FFFFFF)  // Inner glass container surface
val DarkGraySurface400 = Color(0x1AFFFFFF)  // Intermediate elevation layer 
val DarkGrayCard600 = Color(0x22FFFFFF)     // Elevated active card components
val GlassBorderColor = Color(0x1BFFFFFF)    // Frosted glass white-tint border (approx 10% alpha)

// Glow Accents
val ElectricBlue = Color(0xFF2F7BFF)       // Primary branding action stroke
val CyberCyan = Color(0xFF00E5FF)          // High contrast visual alerts
val SoftOrange = Color(0xFFFF7D29)         // Warm creative focal accents
val MutedAmber = Color(0xFFFFB300)         // Goals metrics completion fill

// Premium Gradients
val GlassGradStart = Color(0x351F1F24)
val GlassGradEnd = Color(0x0F0D0D11)

// Semantic Dark Colors
val DarkTextPrimary = Color(0xFFFCFCFD)     // High-readability display labels
val DarkTextSecondary = Color(0xFFA0A3B5)   // Description paragraphs
val DarkTextMuted = Color(0xFF6B6E80)       // Small annotations
val GlassCardAlpha = Color(0x0AFFFFFF)
